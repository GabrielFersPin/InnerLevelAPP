import React from 'react';

const GuildSettings = () => {
    return (
        <div>
            <h1>Guild Settings</h1>
            <p>Manage your guild settings here.</p>
        </div>
    );
};

export default GuildSettings;